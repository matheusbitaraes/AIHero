{

  "generated_evidences_dir": "data/evidences/gifs",
  "verbose": false,
  "checkpoint": {
    "use_checkpoint": false,
    "checkpoint_folder": "data/training_checkpoints"
  },
  "training": {
    "num_examples_to_generate": 1,
    "num_epochs": 300,
    "train_data_folder": "../data/train",
    "noise_dim": 100,
    "batch_percentage": 0.1,
    "buffer_percentage": 1
  },
  "data_augmentation": {
    "enabled": true,
    "data_augmentation_strategy_pipeline": [
      {"method":"TimeChangeStrategy", "factor": 10},
      {"method":"NoteJoinStrategy", "factor": 2},
      {"method":"OctaveChangeStrategy", "factor": 2}
    ],
    "augmentation_factor": 10
  }
}