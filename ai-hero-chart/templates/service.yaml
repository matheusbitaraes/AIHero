apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-hero-chart.fullname" . }}
  labels:
    {{- include "ai-hero-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-hero-chart.selectorLabels" . | nindent 4 }}
